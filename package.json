{
  "name": "holdshort",
  "version": "0.1.0",
  "private": true,
  "description": "Flight briefing decision support. A study and planning aid — not an operational tool.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "holdshort": "tsx packages/core/src/cli/main.ts",
    "corpus:metar": "tsx packages/core/scripts/corpus-report.ts --kind metar",
    "corpus:taf": "tsx packages/core/scripts/corpus-report.ts --kind taf",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "start": "npm run start -w apps/api",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "eval:notam": "tsx packages/core/scripts/notam-eval.ts"
  },
  "devDependencies": {
    "tsx": "^4.23.13",
    "typescript": "^5.7.2"
  }
}
